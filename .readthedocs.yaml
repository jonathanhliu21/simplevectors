version: 2

build:
  os: ubuntu-20.04
  tools:
    python: "3.8"
  jobs:
    pre_build:
      - doxygen

sphinx:
   configuration: docs/source/conf.py

python:
  install:
  - requirements: docs/requirements.txt
